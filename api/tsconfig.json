{
  "//": "Type-checks the Vercel serverless functions (api/) and their packages/core imports the way the @vercel/node builder does — Node ESM with .js-suffixed relative imports. Without this, api/ was checked by nothing locally and type errors only surfaced on deploy. Test files are excluded (they aren't deployed and use bundler-style extensionless imports).",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "types": ["node"],
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["**/*.ts"],
  "exclude": ["**/*.test.ts"]
}
